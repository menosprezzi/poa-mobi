import { InjectionToken } from '@angular/core';

/***
 * Used to provide the API URL for this domain.
 */
export const TRANSPORTATION_WEB_API_URL = new InjectionToken<string>('TRANSPORTATION_WEB_API_URL');
